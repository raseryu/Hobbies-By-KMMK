# No-Node UI (Admin Dashboard)

Your existing app in `groove-gather-main/` is a Vite/React project, which **requires Node.js** to run or build.

If you want a UI that does **not** use Node.js at all, use the static admin UI here:

- `admin-static/index.html`

Open it directly in a browser (no server needed).

